extends layout


block content
  h1= title
  p Welcome to #{title}
  p.f1
    | This is one way to do a 
    | freaking paragraph.
  p.f2
    | This is another way to do 
    | a freaking paragraph.
  div#gamescreen
  script(type='text/javascript').
   $( document ).ready( function() {
     console.log( "ready!" );
     $.getScript( 'javascripts/WssMain.js', function()
       {
         onInit();
       });
   });